# shellcheck shell=bash

# OpenCode session shortcuts
alias opencode='ocx opencode'
alias oc='ocx opencode'
alias oc:boost='ocx opencode -p boost'
alias oc:regular='ocx opencode -p regular'
alias oc:go='ocx opencode -p go'
