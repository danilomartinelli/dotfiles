gf() {
  local branch=$1

  if [[ -z $branch ]]; then
    print -u2 'Usage: gf <remote-branch>'
    return 1
  fi

  if git show-ref --verify --quiet "refs/heads/$branch"; then
    git switch "$branch"
  else
    git switch --track "origin/$branch"
  fi
}
