#!/bin/sh
#
# Apply the macOS defaults from this checkout.

set -e

SCRIPT_DIR=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd -P)
DOTFILES_ROOT=$(dirname "$SCRIPT_DIR")

exec "$DOTFILES_ROOT/_macos/set-defaults.sh"
