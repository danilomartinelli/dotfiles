#!/bin/sh
#
# Show the diffstat of everything you haven't pushed yet.

set -e

# shellcheck disable=SC1091 # Loaded from the adapter checkout beside this script.
. "$(CDPATH='' cd -P -- "$(dirname -- "$0")/../_scripts" && pwd)/adapter-checkout.sh"

# shellcheck disable=SC1091 # Loaded from the resolved dotfiles checkout.
. "$DOTFILES_ROOT/git/_branch-state.sh"

branch=$(_dotfiles_git_state_current_branch git)
_dotfiles_git_state_require_origin_tracking_branch git "$branch"

count=$(_dotfiles_git_state_ahead_count git "$branch")

if [ "$count" -eq "1" ]; then
  s=''
else
  s='s'
fi

git diff --stat \
  "refs/remotes/origin/$branch..refs/heads/$branch"
echo " $count commit$s total"
