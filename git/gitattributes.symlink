# Normalize line endings: text files commit as LF regardless of how they
# were written locally.
* text=auto eol=lf

# Wire image files to the spaceman-diff driver declared in gitconfig.
*.png diff=spaceman-diff
*.jpg diff=spaceman-diff
*.jpeg diff=spaceman-diff
*.gif diff=spaceman-diff
*.ico diff=spaceman-diff
