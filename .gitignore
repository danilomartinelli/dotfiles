# Envs
*local*
!*.example

# Package managers
Brewfile.lock.json

# IDEs
.vscode/
.idea/

# Local agent workspace state
.context/

# Wrangler writes a per-directory cache (account id, auth state) wherever it
# is invoked. It is machine-local and carries account identifiers.
.wrangler/

# Dump files
*.dump
